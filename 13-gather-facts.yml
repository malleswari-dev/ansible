- name: conditions
  hosts: local
  connection: local
  # anisble collects the info of target server, before it executes any other fact
  tasks:
  - name: facts
    ansible.builtin.debug:
      msg : "{{ ansible_facts }}"